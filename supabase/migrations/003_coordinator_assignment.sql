-- שיוך רכזת↔מועמד (נתי 20.8.2026, עדיפות 1).
-- העמודה מאפשרת: (א) לרכזת לראות רק את שלה; (ב) למועמד לקבל את הטלפון
-- והיומן של הרכזת *שלו*; (ג) לדף מנהל התוכנית לשייך באמת, לא בדפדפן.
--
-- להרצה בדשבורד Supabase → SQL Editor.

alter table candidates add column if not exists coordinator_id text;

-- אין צורך בשינוי RLS: המועמד קורא את השורה של עצמו (כולל השיוך),
-- והכתיבה נעשית רק מצד השרת (SUPABASE_SECRET_KEY) דרך דף מנהל התוכנית.
