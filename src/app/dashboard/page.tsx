/**
 * /dashboard — בוטל (נתי 25.8). מגירת המסע בטאב "המסע" החליפה אותו:
 * שני "מסעות" (דף + מגירה) היו מבלבלים, וכל צמתי המסע ממילא עקפו את הדף
 * ("המסך הבא הוא הפעולה הבאה", 20.8). מה שהדף עשה עבר הלאה —
 * גזירת השלב וסנכרונו ל-DB עברו ל-ResumeTracker, ההתמצאות למגירה.
 * הקישורים הישנים (יש רבים) מקבלים רידיירקט לזיכרון המיקום.
 */
import { redirect } from "next/navigation";

export default function DashboardRedirect() {
  redirect("/");
}
