# Techcareerly — הוראות לקלוד

## מה הפרויקט הזה?

**Web App היברידי** שמלווה מועמדים במסע בחירת קריירה טכנולוגית.
המערכת משלבת אינטראקציה דיגיטלית עם פגישות פרונטליות עם רכזת.

---

## ארכיטקטורה טכנית

| שכבה | כלי | תפקיד |
|---|---|---|
| Frontend | React / Next.js | ה-Web App של המועמד |
| Database | Supabase | אחסון נתוני המועמד ומשימות |
| Automation | Make.com | לוגיקה עסקית, Webhooks, ניתוח AI |
| CRM | Monday.com | ממשק הרכזת, התראות, סטטוסים |
| AI Co-pilot | Claude API | צ'אט תמיכה למועמד |

---

## שמות השלבים — מקור אמת יחיד

**`src/data/journey.ts` הוא המקור היחיד לשמות השלבים.** לפני 13.8.2026 היו חמש
רשימות סותרות בחמישה מקומות. שתי שפות בכוונה: `candidate` (מה שהמועמד רואה —
פתיחת חשבון · היכרות · טעימות הייטק · בחירת מסלול · מלגות והרשמה · סטודנט/ית)
ו-`org` (PRD ודוחות — טרום אינטייק · אינטייק · חשיפה · מסלול לימודים ·
לוגיסטיקה ומלגות · רישום). **אף פגישה איננה שלב** — כל שלב הוא תקופת עשייה,
ושלושת האמצעיים נסגרים בפגישה. אין להוסיף רשימת שמות חדשה בשום קומפוננטה.

---

## מסע המשתמש (6 שלבים)

1. **טרום אינטייק** — SMS/WhatsApp → הורדת האפליקציה → שאלון בסיס
2. **אינטייק חי** — פגישה פרונטלית → רכזת מאשרת במאנדיי → פתיחת מפת הדרכים
3. **חשיפה** — סימולציות מקצועיות → ניתוח AI → פגישה 2
4. **מסלול לימודים** — חקר מוסדות → פגישה 3: נעילת מסלול
5. **לוגיסטיקה** — הכנה פיננסית ומלגות
6. **רישום** — אימות רישום סופי → סטודנט רשום

פגישות עם רכזת (שלבים 2, 3, 4) מסומנות בכתום ב-PRD.

---

## מסכי האפליקציה

- **Login** — מסך הזדהות
- **Dashboard** — מפת הדרכים (מרכז הניווט)
- **Learning** — מרכז למידה ומשימות
- **AI Chat** — צ'אט Co-pilot
- **Squad** — קיר השראה וקהילה
- **Contact** — קשר עם הרכזת

---

## לוגיקת ה-AI (Nudge System)

המערכת מזהה חסמים ומתריעה לרכזת:

- **חוסר פעילות > 72 שעות** → סטטוס "At Risk" במאנדיי
- **כישלון במשימה 3 פעמים** → משימה דחופה לרכזת
- **מילות תסכול בצ'אט** → ניתוח סנטימנט → "Manual Intervention Required"

---

## מצב הפרויקט

- **Production:** https://hasifaapp.vercel.app
- **GitHub:** https://github.com/natirot86-coder/techcareerly
- **Stack:** Next.js 16 + Tailwind v4 + TypeScript
- **Backend:** ישראל (Israelman) — Supabase, Auth, Make.com
- **Supabase מחובר ועובד** (תוקן 12.8.2026 — המשתנים היו שמורים עם BOM ו-`\r\n`).
  קיימות: candidates, tasks, simulation_progress, domain_rankings, chat_messages, nudges.
  **המיגרציה רצה 14.8.2026** (דרך הדפדפן של נתי) — שלבים 3–5 ו-SCCT מסונכרנים.
  בדרך התגלה ותוקן: `chosen_domain` מעולם לא הייתה בסכימה — שמירת התחום נכשלה בשקט
- **מוכן:** כל 6 השלבים, מרחב המתנה, 4 לוחות ניהול, 3 תצוגות מפה

## כללים לעבודה

- שפת הקוד: **אנגלית** (משתנים, פונקציות, קבצים)
- שפת תגובות: **עברית**
- סגנון קוד: פונקציונלי, נקי, ללא over-engineering
- לפני כל שינוי גדול — קרא את הקבצים הרלוונטים קודם
- PRD.md הוא מקור האמת לארכיטקטורה
- README.md מתעדכן אחרי כל שלב
- שלב שלב — לא לבנות מסך הבא לפני אישור

---

## ✅ סטנדרט — כל תחום שמוטמע חייב לכלול

אושר ב-28.7.2026. כל תחום חדש (data, code, cyber, networks...) שנבנה באפליקציה **חייב** לעמוד בחמישה קריטריונים:

1. **מפת מסע נעולה** — ב-`[domain]/page.tsx`: שלבים נפתחים בהדרגה (sim → learn/analytics → mystery → experience)
2. **כפתור "מיציתי את הטעימה"** — בסוף כל שלב, מסמן localStorage ועובר לשלב הבא
3. **כלי עיבוד חוויה (SCCT)** — `/explore/[domain]/experience/page.tsx` עם 6 שאלות (עניין / self-efficacy / ציפיות תוצאה)
4. **סרטוני העשרה קצרים בעברית** — בכל שלב, דרך LearnMore component
5. **בלוק הקשר תעשייה** — לפני SimTeaser: כמה משרות, סוגי תפקידים, שכר, נתיב כניסה

**דוגמה מלאה קיימת:** תחום דאטה (`/explore/data/*`)

---

## 📁 תחומים קיימים + סטטוס

| תחום | דף domain | סימולציה | מרכז למידה | כלי חוויה | סרטונים |
|------|-----------|----------|------------|-----------|---------|
| data | ✅ מלא | ✅ | ✅ analytics+mystery | ✅ | 🔄 בתהליך |
| code | ✅ חלקי | ✅ LearnMore | ❌ | ❌ | ✅ |
| cyber | ✅ מלא | ✅ alert triage | ✅ day+mystery | ✅ | ❌ |
| networks | ✅ מלא | ✅ | ✅ day+mystery | ✅ | ✅ |
| qa | ✅ מלא (ב-[domain] הדינמי) | ✅ | ✅ day+mystery | ✅ | ❌ |
| hardware | ✅ מלא (20.8) | ✅ אצוות קבלים | ✅ day+תכנון+mystery | ✅ | ✅ 6 מאומתים |

**חומרה (20.8):** האנלוגיה היא מים-בצינורות בכל הזרימה (מתח=לחץ). היום-בחיי
מטעים **את שני הכובעים** — מעבדה (הנדסאי) ותכנון (מהנדס) — כי העמדה דוחפת
לתואר והתכנון הוא מה שמצדיק אותו. בלוק הנדסאי-מול-מהנדס עם נתוני למ״ס
ותווית האופק. אין דלת בוטקאמפ לחומרה — מוצג ככנות, לא מוסתר.
**מכאניקה בסיווג הרשמי** (קבוצת ההנדסות ב-tech-professions) — נכנסה תחת חומרה.

---

## 🏛️ מודל הנתונים — שלוש ישויות וכלל הדלת (13.8.2026)

| ישות | קובץ | מה היא |
|------|------|--------|
| **מוסד** | `src/data/institutions.ts` (69) | מי שמלמד — עם `city` ו-`address` מ-19.8 |
| **תוכנית** | `src/data/scholarships.ts` (29) | מי שמממן/מפנה — מלגות (`scholarship`) ותוכניות מעטפת (`program`) |
| **קורס** | `src/data/courses.ts` (12) | הדבר הקונקרטי: מחזור, מחיר, דף נחיתה. **קורס = מוסד × מעטפת** |

**כלל הדלת: הדלת הספציפית גוברת על השער הכללי.** מוסד נושא `programIds` (רשימה, מ-19.8)
לתוכנית-הדלת שלו והתצוגה מובילה בה — "סיקט · בן-גוריון", לא "בן-גוריון".
תוכנית לעולם אינה רשומה כמוסד (לוחמים להייטק, סיקט, אדמאס, קרן אור הועברו).

**קורסים:** רק הכשרות מקצועיות, ורק כשהדף הספציפי טוב מהכללי. מצב המחזור
**נגזר מהתאריך** (`courseState`) — קורס שפג נעלם מהמועמד מעצמו ומופיע
בפס ההתראות באדמין. לא ממציאים תאריך חדש — שואלים.

**דירוג תוכניות (`scoreFunding`):** מודד מה מחזיק אדם עד הסוף — מעטפת, כסף,
השמה, נגישות — **ומחזיר סיבות, לא רק מספר**, כדי שרכזת תוכל להגן על הדירוג.
אימות הוא תקרה: `needs-check` לא עוקף מאומת. הדירוג לפי מסלול בלבד —
עתידאים (מה"ט) לא מתחרה בעתידים (תואר).

**מחקר האגף לחיילים משוחררים (13.8.2026):** האתר מרונדר ב-JS — חובה
agent-browser, לא fetch. הניב 7 קורסים כמעט-חינם (השתתפות 980–2,000 ₪
מהפיקדון) ו-3 מלגות, כולל מה"ט 90% שהופך הנדסאים כמעט חינמי למשוחררים.

---

## 🚪 שלב 2 — היכרות ומרחב ההמתנה (12.8.2026)

- **`/waiting`** — ציר: שאלון ← ימי ההמתנה ← הפגישה. ההכנה פתוחה תמיד;
  **"שתי הדקות" (לא "טעימה"!) נעולות עד קביעת פגישה** — מנעול נראה, לא מוסתר
- שתי הגנות פיזור בסימולציית ה-AI מסומנות בקוד "אין להסיר"
- **אף אחד לא מסמן נוכחות:** מועד הפגישה נשמר מ-Cal.com, ושעה אחריו המסך
  שואל "איך היה?" — "לא הצלחתי להגיע" ← קביעה מחדש + דגל at-risk
- השלב בדשבורד נגזר ממה שקרה (פגישה נקבעה/התקיימה), לא מלחיצת כפתור
- שלוש הפגישות ב-`src/data/meetings.ts` — הרכזת דרך `coordinatorFor()`

---

## 🧭 שלב 5 — מלגות והרשמה (`/plan`, 12.8.2026)

- **משימה בגודל ישיבה אחת** — משימה גדולה מתפרקת. זה אילוץ על מחולל התוכנית
- **חשבון במקום הרגעה** — המספר (שכ"ל − מלגות = פער), לא "אל תדאג"
- **אין מסך חששות ואין שאלון עליהם** — פחד נמדד בהתנהגות (דדליין שעבר,
  3 פתיחות בלי סגירה) ועובר לרכזת. לא מאבחנים ולא מטפלים באפליקציה
- **הארון לא שומר קבצים** — סטטוס ומיקום בלבד. החזקת מסמכי זהות היא אחריות
  שהוחלט (13.8) לא לקחת בשלב זה
- עדכון לרכזת נבנה מעצמו מהסטטוסים ונשלח בוואטסאפ ידנית

---

## 👩‍💼 מסך הרכזת (`/admin/coordinator`, 14.8.2026)

**כוכב הצפון: "מי צריך אותי היום" — תור חילוץ, לא CRM.** רשימה ממוינת לפי
דחיפות: פספוס פגישה › דדליין שעבר › שקט 72 שעות › משימה תקועה › עניין גבוה
עם מסוגלות נמוכה (שיחה מחזקת). כל שורה: אדם + סיגנל במילים + ציר זמן.
**בכוונה אין בו ציונים והשוואות** — המסך משרת התערבות, לא הערכה.
טכנית: `/api/coordinator` בצד שרת עם `SUPABASE_SECRET_KEY` (עוקף RLS),
שער `COORDINATOR_CODE`. שני המפתחות ב-Vercel בלבד, לעולם לא `NEXT_PUBLIC_`.

## 📡 מה נמדד — ואיך לא לשקר במסך (17.8.2026)

**הכלל: מסך שמציג מספר שאף אחד לא כותב אליו הוא מסך שמשקר בשקט.**
שלושה כאלה תוקנו, ומהם נגזרים הכללים:

- **`last_active_at` נוגעים בו מכל ניווט** (`touchActivity`, מרוסן לשעה
  ב-localStorage). קודם התעדכן רק בהרשמה ובמעבר שלב — ולכן מי שנכנס
  כל יום נראה כאילו נעלם. `/api/coordinator` לוקח **מקסימום מול האירוע
  האחרון**, שחתום בשרת, כדי ששעון שגוי במכשיר לא ייצור "נעלם לחודש"
- **`open_count` נספר** בחזרה למשימה העוגן ובפתיחת משימה מקופלת.
  `syncPlanTasks` עושה **upsert ומוחק לפי הפרש** — הגרסה הישנה מחקה
  הכל בכל שמירה, ולכן שום מונה לא יכול היה לשרוד
- **נטישה לא נרשמת, היא מוסקת.** אין בנייד אירוע "יצא", ולכן לעולם
  אין אירוע `abandon` — מסיקים מ"הגיע לצעד N ולא ל-N+1"

**אירועים שנשלחים:** `meeting_open` · `meeting_calendar_ready` ·
`meeting_booked` · `meeting1_checkin` · `sim_start` · `sim_step`
(עם `concept` — "צעד 4" חסר ערך, "תרגיל SQL" אומר מה לתקן) ·
`scct_step` · `scct_done` · `paths_question` · `paths_quiz_done` ·
`paths_blocker_open` · `paths_solution_click` · `plan_task_open` ·
`plan_money_opened`.

**`paths_blocker_open` מודד שכיחות ולא קליקים** — מסך החסמים מציג את
כולם פתוחים, אז אין בו פתיחה. לא מוסיפים אינטראקציה כדי לשרת מדידה.
השם נשאר כי `admin_stats()` מצטבר לפיו וזה חוסך מיגרציה.

**הצבירה של האירועים החדשים ב-`/api/funnel`** (צד שרת, מפתח סודי) ולא
ב-`admin_stats()` — היא נכתבה לפני שהם היו קיימים, והוספה שם דורשת
מיגרציה. סופרת **אנשים שונים ולא אירועים**, כדי שאדם אחד לא ינפח.
מזינה שלושה כרטיסים חדשים באנליטיקות: נטישה לפי צעד בסימולציה,
באיזו משש השאלות עוצרים, ומשפך הפגישה (הגיע ← היומן נטען ← קבע).
`meeting_calendar_failed` מופרד בכוונה: כשל טכני נראה בדיוק כמו היסוס.

**מסך הרכזת:** "בקצרה" לפני הציר · אירועים כמשפטים בעברית · ביקורים
(פער 30 דק׳ = כניסה חדשה) עם משך · רצף צעדים מכווץ לשורה שמראה עד
לאן הגיע · **שתי שתיקות נפרדות**: "נעלם" מול "נכנס ולא מתקדם", והשנייה
דחופה יותר — היא מי שתקוע ולא יבקש עזרה מעצמו.

## 🔁 זיכרון מיקום (14.8.2026)

**מי שיוצא וחוזר — חוזר בדיוק לאיפה שהיה.** `ResumeTracker` ב-layout שומר
כל ניווט (חוץ מ-admin/map/reset/login ומצבי `?demo`/`?reset`), ודף הבית `/`
מחזיר למיקום השמור במקום לזרוק לאונבורדינג. עומק פנימי באחריות כל דף:
paths משחזר phase, plan זוכר טאב (`plan-last-view`).

## 🎯 שלב 4 — מסלולי לימוד (`/paths`)

נבנה מחדש ב-11.8.2026. **תשע מסכים.** הכללים שנקבעו ואסור לשבור אותם בלי החלטה מפורשת:

### כוכב הצפון של השלב
> להביא אדם שעדיין לא סגור — לא על הייטק ולא על התחום — לנקודה שבה הוא **רואה** מסלול
> אחד קונקרטי, **יודע איך הוא נכנס לחיים שהוא חי היום**, ו**מאמין שהוא מסוגל** לעבור אותו.

האפליקציה לא רושמת למוסד. היא מביאה אותו **מוכן לפגישה 3**, ושם הרכזת סוגרת.

### עמדה, לא ניטרליות
**ממליצים על תואר במפורש** — כי הוא מעלה את הסיכוי להתקבל, פותח משרות שוות יותר,
ומשרת סטודנט מתחילה לפני הסיום. יחד עם המקרים הכנים שבהם הוא לא נכון.

- מנוע ההמלצה הוא **ניקוד משוקלל** (`WEIGHTS` ב-`paths/page.tsx`), לא וטו.
  התואר מקבל בסיס 5 וכל תיקו. אומת על 729 צירופים: תואר 52% · מה"ט 10% · הכשרה 38%
- **מה"ט תמיד אחרון** וממוסגר לקריירה ביטחונית/ממשלתית/חומרה
- **דירוג תחומים לפי עניין ולא לפי מסוגלות** — לדור ראשון תחושת המסוגלות נמוכה
  באופן שיטתי, ודירוג לפיה יסתיר בדיוק את מה שהוא רוצה

### כללי תוכן
1. **לא ממציאים.** לא טלפונים, לא סכומים, לא מוסדות. מה שלא אומת → `status: "needs-check"`
   והערה פנימית. (בסשן הזה הומצאה קרן שלא קיימת — הוסרה)
2. **מפנים ליחידת התמיכה, לא למדור הרישום.** מדור רישום עונה "אינך עומד בתנאים"
   ולא מכיר מסלולי קבלה חלופיים — שיחה כזו יכולה לסיים את המסע
3. **מתי מתחילה הכנסה, לא כמה.** אין נתון ישראלי מאומת על שכר סטודנטים
4. **רצועות זמן, לא נקודות.** "מסוף שנה א׳", לא חודש מדויק
5. **היעדר הוא מידע.** "לא מצאנו מסלול X בתחום הזה" — מנוסח כ"לא מצאנו" ולא כ"לא קיים",
   כי הרשימה שלנו חלקית ואסור שהאפליקציה תסתור רכזת
6. **אפס טקסט על התרשים.** שם תחנה בלבד. העומק במסך הנפרד

### מקורות אמת
- `src/data/institutions.ts` — 29 מוסדות. **שדה `domains` קובע למי מוצג מה**
- `src/data/routes.ts` — מסלולי כניסה למשרה ראשונה לפי תחום
- `src/data/tech-professions.ts` — הסיווג הרשמי של משרד העבודה למקצועות טק
- **`docs/research-findings.md`** — כל הממצאים: מקורות, תאריכי אימות, מטריצת
  צבירת מלגות, מה שהתברר כשגוי, ומה שנשאר בלתי ניתן לאימות. **לקרוא לפני
  שמוסיפים או משנים נתון על מוסד או מלגה.**
- `/admin/institutions` — עריכה + ייצוא JSON. אין בקאנד, אז עריכה נשמרת מקומית בלבד

### לבדיקה מהירה
`?reset=1` מנקה הכל · `?demo=1&phase=X` קופץ ישירות למסך עם נתוני דמו
(`intro` `quiz` `result` `routes` `blockers` `institutions` `prep` `research` `done`)

---

## 🧭 שלב 4 — מה נקבע ב-19.8.2026

### השאלון מודד גם שאיפה, לא רק מגבלות
שבע השאלות הישנות מדדו **מה עוצר** — ולכן התואר יכול היה רק לרדת, וכל שאלה
שהוספנו הורידה אותו עוד. נוספה השאלה שהוא **יכול לזכות בה**: *"מה חשוב לך
יותר — להתחיל מוקדם, או לפתוח את מגוון התפקידים והשכר"*, עם **"עוד לא יודע/ת"
שלא נענשת**.

**שאלת ההשכלה הוחלפה בבחירה מרובה** — "מה יש לך ביד": בגרות · מתמטיקה 3/4/5
יח׳ · מגמה ריאלית · אנגלית · פסיכומטרי (מרוצה / לא מרוצה) · תואר. `education`
**נגזר** מהצ׳יפים ולא נשמר בנפרד, כדי שמנוע הניקוד ומסך החסמים לא ייפרדו
מהשאלה שמזינה אותם. **הציון בפסיכומטרי לא נשאל בכוונה** — מספר על מסך מזמין
שיפוט עצמי, ואצל מי שהמסוגלות שלו נמוכה זו נקודת הנטישה.

### כל שינוי משקל עובר בסימולציה
`scratchpad/sim.mjs` מריץ את כל הצירופים ומשווה לתמהיל היעד. **התמהיל אחרי
19.8: תואר 54.3% · מה״ט 4.2% · הכשרה 41.4%** (יעד 52/10/38). מה״ט מתחת ליעד
ונשאר כך בהחלטת נתי. **הוספת שאלה בלי להריץ את הסימולציה כבר הורידה את התואר
מ-52 ל-48 פעם אחת** — לא לחזור על זה.

### מספרים אומרים מה הם, או שהם לא מוצגים
"75% ממסיימי התואר עובדים בהייטק" ולא "75% בטק". "שכר חודשי, 5–6 שנים אחרי
התואר" — מספר בלי אופק נקרא כמשכורת התחלתית. **תווית הכניסה הוחלפה בדרישה
עצמה** ("מתמטיקה 4–5 יח׳ · פסיכומטרי") כי "כניסה תחרותית" נראתה כמו נתון
והקריטריון שהפיק אותה מעולם לא נרשם.

**המחירים נגזרים מהמאומת בלבד:** תואר 12,017 ₪ לשנה (מפוקח, זהה ב-11 מוסדות),
הכשרה 980–6,000, מה״ט 9,300–11,100. **בוטקאמפ פרטי הוצא מהטווח** — נדיר, לא
ממליצים עליו, ואין סיבה שיעגן מספר שמפחיד מתואר.

### האתגר — בפאנל בלבד
לכל תואר יש `challenge` ו-`challengeHelp`. **מוצג רק בפאנל שנפתח ולעולם לא
בכרטיס המצומצם:** כרטיס נסרק בשנייה ומשמש לסינון, ולכן ציון קושי עליו היה
מסליל את כולם לתואר הקל. **ומשפט ולא דירוג** — מספר מזמין השוואה, משפט מזמין
להתכונן. תמיד עם מה שמקטין אותו, אחרת זו הרתעה.

### תואר ← מוסדות, לא שתי רשימות
הכרטיס המצומצם נושא שלושה מספרים בלבד, ולחיצה פותחת פאנל **מתחת לשורה שלו**
כדי שהחיבור החזותי יישמר. הקטלוג הכללי מקופל כברירת מחדל.

## 🚪 מודל הידע — שני תיקונים (19.8.2026)

**מוסד נושא `programIds` — רשימת דלתות, לא אחת.** לבן-גוריון נכנסים גם דרך
סיקט וגם דרך עתידים, ואלה שתי אפשרויות שונות לאותו אדם. **מוסד עם שתי דלתות
פתוחות לתואר מודגש בירוק ועולה לראש** — אם אחת לא מסתדרת, השנייה פתוחה.
עתידים אומתה בשמונה מוסדות.

**`openToAllDegrees` — המצב השלישי.** אדמאס וסיקט הן תוכניות ליווי ברמת
המוסד, ולשאול אותן "לאילו תארים" זו שאלה שגויה. **ריק עדיין אומר "לא נבדק"
ולא "הכל"** — רק הדגל אומר "הכל".

**מזהה המוסד הוא המקום ולא הדלת:** `runi` ו-`tau` (היו `runi-keren-or`,
`tau-admas` — אותה מחרוזת שימשה גם כמזהה מוסד וגם כמזהה תוכנית).

## 📍 גיאוגרפיה (19.8.2026)

`city` · `address` · `priority44` בכל מוסד, לצד `location` החופשי. **הסיבה
אינה מפה יפה:** שתי מלגות הפריפריה נקבעות לפי כללים **הפוכים** —
**ייעוד 44** לפי מיקום ה**מוסד** באזור עדיפות לאומית, **פריפריה 45** לפי
כתובת המגורים של ה**מועמד** (5 מתוך 6 שנים). כל אחת שווה שנה א׳ במימון מלא.

**שם היישוב לפי קובץ הלמ״ס** (data.gov.il), כדי להתלכד עם המזהה הרשמי.

**המיקום שייך לקורס, לא למוסד** — בנתיבי אודי, קווליטסט וטק-קריירה מפעילים
מחזורים בערים שונות בלי קשר לכתובת המשרד. "יש סניף אורט באשקלון" **אינו**
"קורס הסייבר רץ באשקלון".

**במסך המפה שלוש קבוצות ולא אחת:** 📍 על המפה · 🌐 אונליין (רשימה נפרדת —
זו תכונה ולא חוסר) · 🇮🇱 פועלים בכל הארץ (רשימה, בלי לבחור סניף אקראי).
**וקבוצה רביעית באדמין בלבד:** מי שלא נבדק. ההבדל בין "לא רלוונטי" ל"לא
נבדק" גלוי לנתי ובלתי נראה למועמד.

## ⏳ חשבון השעות (19.8.2026)

`annualHours` בכל מלגה, ומסך `/plan?view=money` מסכם. רוטשילד 400 שעות בשנה
(עשר שבועיות), ISEF 80, פר״ח 100. **מי שצובר שלוש מלגות מתחייב ליותר ממה שיש
לו** — ואז הוא לא נושר ממלגה, הוא נושר מהלימודים. ההשוואה נעשית מול מה שהאדם
**עצמו** אמר בשאלון על שעות פנויות; סף כללי היה שרירותי.

## 📋 מה שנשאר פתוח (19.8.2026)

| פריט | תלוי במי |
|------|----------|
| ~~המיגרציה~~ ✅ רצה 14.8 · ~~חיבור שלבים 4–5 + SCCT~~ ✅ מסונכרן | — |
| ~~הדבקת SUPABASE_SECRET_KEY בוורסל~~ ✅ 17.8 — מסך הרכזת חי ומחזיר נתונים | — |
| ~~שלושת הסיגנלים השבורים~~ ✅ 17.8 — ראה "מה נמדד" למטה | — |
| **`fail_count` ב-`tasks`** — אין באפליקציה מושג של "כישלון" (סימולציה לא ניתן להיכשל בה, במכוון). להגדיר או למחוק כמו `status` | נתי |
| ~~מסך הקורסים העטופים למועמד~~ ✅ · ~~בוחר התארים~~ ✅ 19.8 | — |
| ~~כתובות מדויקות~~ ✅ 19.8 — 49 עם כתובת מלאה (3 סוכני חיפוש מקבילים). לא מפרסמים כתובת: טק-קריירה (!), אנליזה, Google×רייכמן, יניב ארד. קורסי האגף — שאלה לרכזות. בונוס: תוקן city של טק-קריירה חיפה←לוד | נתי: כתובת טק-קריירה |
| **מיקום ברמת הקורס** — היום נגזר מהמוסד, וזה שגוי לגופים עם מחזורים בכמה ערים | קלוד |
| **שאלת יישוב מדויקת למועמד** — פותחת את פריפריה 45 | קלוד |
| **מסך מפה** — שלוש הקבוצות | קלוד |
| מלגות שנבדקו וממתינות להחלטה: לוויתן · פועלים להצלחה · דרך עמ״י · נפש יהודי · חב״ד | נתי |
| מלגות שלא נפתחו: אור לחינוך (403) · המקום תרבות ורוח · אתנחתא · סל״ע (נסגרה לתשפ״ז) | נתי |
| גודמן — **תואר שני ודוקטורט בלבד**, לא רלוונטי לקהל הנוכחי | — |
| webhook של Cal.com (`/api/cal-webhook`) — אחרי שהמפתח בוורסל | קלוד |
| אישור 60+ פריטים בלוחות: מוסדות · מלגות · קורסים | נתי |
| בחירת מפה: `/map` · `/map/flows` · `/map/grid` | נתי |
| טלפונים: בנתיבי אודי (חינם לעוטף?) · עתידים/עתידאים · IITC · קווליטסט · אחווה · תוכנית יואל | נתי + רכזות |
| "תלתן" לא אותר — צריך שם מדויק או קישור | נתי |
| תחום code — חסרים day/mystery/experience | קלוד |

## 🎬 סרטוני דאטה — IDs מאושרים

| מיקום | Video ID | נושא |
|-------|----------|------|
| `/explore/data` (domain page) | `vemlok2E87o` | מה זה דאטה אנליסט ב-60 שניות |
| `/explore/data/learn/mystery` | `Vb60d3w3Rzc` | מבוא לשפת SQL |
| analytics — step 2 | TBD (נתי מטפל) | ניקוי נתונים |
| analytics — step 3 | TBD (נתי מטפל) | ויזואליזציית נתונים |

---

## 🌐 סרטוני רשתות — IDs מאושרים (ערוץ @Adjacentnode — עברית)

| מיקום | Video ID | נושא |
|-------|----------|------|
| `day` — career RevealCard | `rrOPnWr3JSs` | 5 דברים שמהנדסי רשת לומדים בעבודה (4 דק') |
| `day` — trace-result RevealCard | `8f5RhuMhBwY` | Traceroute — כנראה שאתם קוראים בצורה שגויה (4 דק') |
| `day` — ping-result RevealCard | `7X8D5anJCRo` | Ping ו-Traceroute הסבר בעברית |

## 🌐 השראה לתחום רשתות

קורס קמפוס IL: https://campus.gov.il/course/cs-gov-cs-reshatot101/
נושאי ליבה: IP, DNS, מודל 5 שכבות, HTTP, TCP/IP, routing, subnets
סגנון: טכני + ויזואלי + אינטראקטיבי


## 🚪 שערי המסע — מה נקבע ב-20.8.2026

**בכל צומת, המסך הבא הוא הפעולה הבאה.** ארבעה יישומים:
- **סוף אונבורדינג ← ישר יומן פגישה 1** (`/contact?m=1&welcome=1`) עם משפט מסגור אישי. לא דשבורד.
- **"היה טוב" בצ'ק-אין ← ישר `/explore`.** בלי כרטיס ביניים.
- **שער הכיוון בשלב 4** — בחירה מפורשת של 1–2 תחומים, **בלי "עוד לא סגור"**
  (החלטת נתי: האופציה נותנת לגיטימציה לחוסר ביטחון). הפיצוי: תקיעה בשער
  נמדדת — `paths_domain_gate` בלי `domain_committed` יומיים ← סיגנל רכזת.
- **שער המוסד בשלב 5** — מוסד אחד + גיבוי מסומן (שניים שקולים = החלטה שלא
  התקבלה). `plan_inst_gate` / `institution_committed`.

**מצב הכנה לפני פגישה 2:** השאלון והיכרות המסלולים פתוחים (אילוצים לא תלויים
בתחום); שער הכיוון, מוסדות וחסמים נעולים עד שהפגישה התקיימה (נגזר מ-meeting-2-at,
לא מכפתור). אותו עיקרון כמו מרחב ההמתנה. `?demo` עוקף.

**ריבועי הטעימות:** 4 (לפי תחומים עם טעימה אמיתית), קו סף אחרי 2, נוסח הזמנה
אחרי הסף — לא חגיגת סיום. עיגון עובד לשני הכיוונים.

**מסך הסיום של שלב 4 מציג את הבחירות שלו** — לא את ההמלצה שלנו תחת "ההחלטה
שלך". ההמלצה בשורה נפרדת, מסומנת.

**שלב 5:** משימת ההרשמה = העוגן, מוצמדת ראשונה ("תנאי לכל השאר") — היא תנאי
מקדים למלגות, לא "משימה בלי תאריך". החשבון מותאם למוסד שנבחר (מוסד פרטי
מקבל הערת אמת), מלגות לחיצות שמורידות מהחשבון, `EXCLUSIONS` מקודדות עם
כרטיס "על מה מוותרים". **חריג יחיד ל"ארון בלי קבצים": אישור הלימודים כן
נשמר** (Supabase Storage, נתיב אישי per auth.uid) — משרד העבודה דורש אסמכתא.
SQL: `supabase/migrations/002_enrollment_docs.sql` — **ממתין להרצה אצל נתי**.

**מסך הרכזת:** ברירת המחדל נשארה תור החילוץ (14.8 בעינה). נוסף טאב "כל
המשתתפים" עם **צ'קליסט 9 אבני דרך נגזר-אירועים** — הצ'קליסט קודם ללוג;
הלוג הגולמי משרת אנליטיקות. `/admin/program` — סגל רכזות + שיוך (מקומי,
עד עמודת coordinator_id). אונבורדינג: **תאריך לידה** (גיל נגזר, יו"ה לרכזת)
+ **שאלת שירות** (קובעת חצי ממפת המימון). נשלחים כאירוע `profile`.

**באג המיזוג באדמין (תבנית!):** לוח שטוען טיוטה מ-localStorage ומחליף בה
את רשימת הקוד מסתיר כל מה שנוסף בקוד. התיקון: הבסיס תמיד הקוד הטרי,
העריכות יושבות עליו לפי id. תוקן במוסדות, בתארים וברוסטר.

**אימותים שנכנסו (20.8):** שכ"ל מפוקח 12,203 ₪ תשפ"ז (היה 12,017=תשפ"ו) ·
אשקלון בלי פסיכומטרי = ממוצע 95 (לא 85!) · "ראויים לקידום" = ציון זכאות,
לא תוספת · טק-קריירה 88% השמה, מחיר לא פורסם (2,000 ₪ בטלגרם) · המכללה
למנהל: **עולים ביחד = שכ"ל מלא** (מדמ"ח/מ"מ/כלכלה, בגרות 90+) לצד תנופה
15,000 שנצברת עם מרום · ייעוד 44 לתשפ"ז נסגרה 15.8.

**סריקת התארים ההפוכה:** 26 מוסדות תואר, כולם ממופים. תואר 14 — **בינה
מלאכותית** (חיפה, בג"ג חדש בתשפ"ז, עברית 528, או"פ). BIU 3←8, בג"ג 6←10,
אונו 1←3. HIT תוקן ממה"ט לאקדמי. הדסה = "המרכז האקדמי הרב תחומי ירושלים"
(jmc.ac.il). קטגוריות ממתינות להחלטת נתי: הנדסת נתונים · יזמות · שיווק
טכנולוגי · מדעי המוח.

**ממתין לנתי (20.8):** מספר וואטסאפ רכזת (`COORDINATOR_WA` ב-plan/page +
roster) · הכרעת "אישור=פרסום" באדמין · 10 מכללות מה"ט מוסתרות · הרצת SQL
של ה-Storage · ספרינט 66 הממתינים לאישור.


## 🛠 מה נקבע ב-23.8.2026

**תשתית:** לקלוד יש Supabase Access Token (קובץ מקומי מחוץ לריפו) — מיגרציות
רצות ישירות דרך ה-Management API, בלי "הדבק SQL". רצו: 002 (Storage אישורים),
003 (coordinator_id), 004 (טבלת coordinators), 005 (cal_bookings).
**סגל הרכזות חי ב-DB** — דף הניהול שומר מיד, בלי JSON. ה-webhook של Cal
(`/api/cal-webhook`) כתוב — ממתין ל-CAL_WEBHOOK_SECRET בוורסל + הגדרה ב-Cal.

**מסע הלקוח לרכזת** (`/admin/coordinator` ← שם משתתף): פרסונה עם פיל
בתנועה/עצירה, גיל+יישוב+שירות+שעון זכאות משוחררים (נגזר מתאריך שחרור+5),
🎂 יו"ה בקרוב, סרפנטינה 12 תחנות נגזרות-אירועים, פאנל תחנה שנפתח אוטומטית
על עצירה, ו"לקראת השיחה" (במה מתעניין · איפה מסתובב במעגלים · כמה פה)
**במקום ציר ביקורים גולמי** — הלוגים הגולמיים לאנליטיקות, לא לרכזת.
`taste_done` מסנכרן השלמות day/mystery/experience מכל התחומים (רטרואקטיבית).

**אונבורדינג v2:** יישוב מגורים ("רוב שש השנים האחרונות" — פותח פריפריה 45) ·
שירות כעובדות: סוג (צבאי/לאומי/משרת/ללא) + חודש שחרור (שעון זכאות) + תעודת
מילואים פעיל. לא שואלים "אתה חייל משוחרר?" — גוזרים.

**track רביעי: prep.** לוינסקי תוקן מ"מוסד תואר" לאפיק הכנה. 7 ישויות מכינה
מאומתות נוספו. ממצאי הזהב: **מכינת חיפה = אפיק מעבר לתואר בלי פסיכומטרי** ·
**בראודה כמעט-חינם + פטור מפסיכומטרי ב-85+** · **הפסיכומטרי של המדינה חינם**
(קמפוס IL) · לוינסקי חינם ליוצאי אתיופיה · מימון האגף למכינה מלא ולא מהפיקדון.
ההשוואה המלאה: מי מחליף בגרות בלבד (ת"א-ב"א, טכניון) מול מי שמחליף גם
פסיכומטרי (חיפה, בראודה 85+, בדרך-לאפקה למסלול תכנות).

**כרטיס מוסד:** שורות "לא אומת" לא מוצגות — שורת אמת אחת במקום ("שאלות
טובות לרכזת"). **מפה:** אזהרה לא צובעת עיר (סדר אגרגציה עירוני נפרד),
מגירה מקובצת לפי כתובת, "מחפשים מסלול ל:X" תמיד מעל המוסדות.
**עוגן ההרשמה נולד משער המוסד** — גם למוסד שלא היה בשורטליסט (+ריפוי לאחור).

**מסך התוצאה (result) בעיצוב מחדש** — פרומפט 3 חלופות נמסר לנתי (כרטיסים
אחידים / היררכיה מפורשת / ציר חיים). הערות ג'מיני אומצו חלקית — המספרים
שלו הומצאו ונפסלו.

**ממתין (23.8):** מוקאפ מסך התוצאה + דשבורד מנהל (פרומפט לפי בקשה) ·
CAL_WEBHOOK_SECRET · טעימות ai/ux/marketing · קורסי המחקר כישויות ·
תיקון השירות בפרסונה למי שנרשם לפני v2.
